This tool is to compare two files via a configuration file. The configuration file indicates the file headers, key columns and etc.

Make sure we have the following packages installed first
# conda install openpyxl PyYaml pyodbc pandas numpy
